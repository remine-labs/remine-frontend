# Dockerfile

develop 브랜치 기준, 프론트 배포용 Dockerfile 입니다.

실행은 하단 명령어로 가능합니다.

docker build -t remine-fe-dev .

docker run --name remine-fe-dev -p 5173:5173 remine-fe-dev

## access

http://localhost:5173
